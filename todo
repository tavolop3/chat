threading: usar threads para mejorar la eficiencia, pthreads?

performance: hashmap fd -> usuario para las operaciones dentro de epoll ¿?

refactor: tomar el username una vez pq se usa en varios lados, usar una variable para data.fd

funcionalidades:
historial: guardar el historial en un archivo, la idea es que despues se genere un archivo por canal
canales: varios canales donde se puedan conectar los usuarios con un id, un proceso o un hilo por canal?
